apiVersion: 1

providers:
  - name: redis-stream-go
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    folder: Redis Stream
    options:
      path: /var/lib/grafana/dashboards
